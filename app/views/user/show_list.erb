
<p> Welcome, <%= current_user.name %>.</p>
<h2> Your Wish List </h2>

<% if @items.any? %>
  <% @items.each do |item| %>
    <div>
      <%= item.name %>
    </div>
  <% end %>
<% else %>
  <p>Your list is empty! Click <a href="/add">here</a> to add an item.</p>
<% end %>
